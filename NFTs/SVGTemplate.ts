const nftName = "SVGs For All";
const symbol = "SVGT";
const contractName = "SVGMinter"; //links to the file name in contracts/<filename.sol>
const contractArgs = [nftName, symbol];

export {
  contractName,
  contractArgs,
}